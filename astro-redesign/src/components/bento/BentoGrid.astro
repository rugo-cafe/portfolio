---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={[
  "w-full max-w-[1400px] mx-auto px-6 md:px-12 grid grid-cols-2 md:grid-cols-4 gap-3 md:gap-4 auto-rows-[minmax(120px,auto)] relative z-10 mb-14 md:mb-16",
  className
]}>
  <slot />
</div>
